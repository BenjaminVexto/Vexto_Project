[pytest]
# Automatisk genkend async tests (uden mark) og giv fixtures session-loop
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session

markers =
    network: tests that require external network access

pythonpath = src
testpaths = tests
norecursedirs = test_playwright_async.py test_pw.py  # Keep this line